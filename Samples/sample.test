config {
  base_url = "http://localhost:8080";
  header "Content-Type" = "application/json";
  header "X-App" = "TestLangDemo";
}

let user = "admin";
let id = 42;

TEST getUser {
  REQUEST {
    METHOD: GET
    URL: "/api/users/$id"
  }
  ASSERT {
    status == 200
  }
}
